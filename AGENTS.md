# AGENTS.md - AIエージェント向けプロジェクト仕様・開発ガイドライン

> [!IMPORTANT]
> **AIエージェントへの必須指示 (Maintenance Protocol)**
> - 本プロジェクトの改修を行うすべてのAIエージェント（Gemini, Claude, Cursor, Copilot, Antigravity等）は、**作業開始前に必ず本ファイルを熟読**してください。
> - **改修前の修正案提示と合意形成（必須）**: 課題の指摘や改修依頼を受けた際、いきなりコードを修正して事後報告するのではなく、**必ず事前に「原因分析」「修正方針・修正案（対象ファイル、具体的なコード差分や変更方針）」をユーザーに提示し、確認・合意を得てから実装・編集に着手**してください。
> - 新機能の追加、仕様変更、アーキテクチャの更新、パッケージの追加等を行った場合は、**作業完了時に必ず本ファイル（AGENTS.md）を最新の状態に更新・同期**してください。
> - **機能追加・改修ごとのGitコミット必須**: 各機能の追加や改修が完了し、テスト（`npm test`）およびビルド（`npm run build`）が通過するごとに、変更内容・意図を明確に記述した**Gitコミットを必ず作成**してください（未コミットの変更を溜め込まず、機能単位で細かくコミットすること）。
> - **Gitプッシュの禁止（ユーザー主導）**: リモートリポジトリへの `git push` はユーザー自身が確認の上で行うため、AIエージェントが勝手に `git push` を実行してはいけません（ローカルでの `git commit` までにとどめること）。

---

## 1. プロジェクト概要

- **名称**: J-Quants 日本株分析・可視化 Webアプリケーション (MVP)
- **目的**: 日本取引所グループ（JPX）が提供する「J-Quants API (V2)」を利用し、日本株の個別銘柄情報の確認、株価・配当利回り・主要投資指標（PER/PBR/ROE等）の表示、およびTradingView製チャートによる過去5年間の日足推移分析を行う。
- **デプロイ先**: **Cloudflare Pages**（静的SPAホスティング ＋ Pages Functions によるリバースプロキシ）
- **開発環境**: Debian Linux 上の **code-server**（Cloudflare Tunnel 経由でローカルPCブラウザからアクセス）

---

## 2. 【最重要】開発ルール・アーキテクチャ上の制約

以下の項目はシステムの根幹に関わる重要な制約です。**AIエージェントはこれらを勝手に変更・削除・破壊してはいけません。**

### ① API通信とCORS回避（直接フェッチの禁止）
- **絶対厳守**: フロントエンド（React）から `https://api.jquants.com/v2` を直接 `fetch` してはいけません。
  - J-Quants API（AWS API Gateway）はブラウザの `OPTIONS` プリフライトに対して `403 Forbidden` を返すため、直接通信するとブラウザで必ず `Failed to fetch`（CORSエラー）が発生します。
- **通信経路**:
  - **開発時**: Vite のリバースプロキシ（`vite.config.ts` の `proxy`）を経由。
  - **本番（Cloudflare Pages）時**: `functions/api/jq/[[path]].ts`（Pages Functions）を経由。
  - フロントコード内では必ず `${BASE_URL}/api/jq/...` 形式でエンドポイントを呼び出すこと。

### ② code-server ＋ Cloudflare Tunnel 開発環境の維持
- `vite.config.ts` で以下の設定を維持すること：
  - `base: command === 'serve' ? '/absproxy/5173/' : './'`
    - code-server のリバースプロキシでは `/absproxy/5173/` がベースパスとして必須（通常の `/` や `./` だとアセットが405/404となり画面が真っ白になる）。
    - 本番ビルド時は Cloudflare Pages のため相対パス `./` に自動切替。
  - `server.host: '0.0.0.0'`（全インターフェースバインド）
  - `server.allowedHosts: true`（Cloudflare TunnelドメインによるHostヘッダ遮断を防止）
  - `server.hmr.clientPort: 443`（HTTPSトンネル経由でのWSSホットリロード接続）

### ③ J-Quants API 契約プランと仕様
- **現在の契約プラン**: **Lightプラン**
  - **レートリミット**: **60リクエスト / 分**（`jquantsApi.ts` の `RATE_LIMIT_MAX = 60` を維持）
  - **データ期間**: **過去5年間分**（約1,220営業日）
  - **データ遅延**: **遅延なし**（当日・直近営業日の最新終値まで取得可能）
- **データ取得仕様**:
  - **日足四本値 (`/equities/bars/daily`)**: 5年分の全データを漏れなく取得するため、`pagination_key` の自動追跡ループ処理を必ず維持すること。
  - **財務サマリー (`/fins/summary`)**: 配当金は **会社予想年間配当（`FDivAnn`）を優先** し、未発表・空欄の場合に **実績年間配当（`DivAnn`）** を使用すること（`extractLatestDps` により一元管理）。配当性向の安易な仮定（例: EPS × 35%等）による推計値の捏造は厳禁。個別分析・ウォッチリスト比較・スクリーナー間で共通の公式開示キャッシュを参照し、画面間での利回りの齟齬を防止すること。
  - **年間EPS & PER / ROE 算出仕様**:
    - **年間EPS & PER**: 通期会社予想（`FEPS`）を最優先とし、未発表・未確定の場合に本決算レコード（`CurPerType === 'FY'`）の確定 `EPS` を使用すること（`extractAnnualEps` により一元管理）。1Q〜3Qの四半期累計EPSで株価を割って異常なPER（例: 100倍超）を算出する誤認計算は厳禁。
    - **ROE**: APIにキーが存在しないため、最新本決算（`FY`）の当期純利益（`NP`）と株主資本（`ShEq` または `Eq`）から算出すること（`calculateRoeFromFins` により一元管理）。
  - **株価指標 & 前日比仕様**:
    - 前営業日の終値が取得できない場合、当日の始値（`bar.O`）で代用して前日比を偽装することは厳禁。前日終値が存在しない場合は前日比・騰落率を `null` とすること。
  - **時価総額 (`MktCap`)**: APIから百万円単位で返却されるため、`formatMarketCap()` で「兆円 / 億円」単位に正しく換算して表示すること。日足の `MktCap` を優先基準として統一すること。

### ④ APIキーの取り扱いとセキュリティ（localStorage管理）
- **公開Webクライアント（ブラウザ）**:
  - APIキーはクライアント向けビルド成果物（JSバンドル）に一切埋め込んではいけません（`import.meta.env.VITE_JQUANTS_API_KEY` をフロントエンドコード内で直接参照・フォールバックすることは禁止）。
  - ユーザーが画面右上「設定」から入力し、ブラウザの `localStorage`（キー: `jq_api_key`）にのみ安全に保存・管理すること（`cacheService.ts`）。
  - Cloudflare Pages 等のホスティング環境の環境変数に API キーを登録してはいけません（静的配信されるJSにバンドルされるのを防ぐため）。
- **ローカルNode.jsスクリプト**:
  - `scripts/update_universe.mjs` や `test/api_verify.mjs` などのローカルCLIスクリプトでのみ、ローカル `.env` の `VITE_JQUANTS_API_KEY` を参照すること。
- `.env` は Git 管理対象外（`.gitignore` に記載）。公開リポジトリにコミットしないこと。

### ⑤ 機能追加・改修ごとのGitコミットの励行
- 新機能の追加、バグ修正、UI改善、仕様変更などのタスクが完了し、`npm test` および `npm run build` で正常動作を確認できた段階で、必ずGitコミットを作成すること。
- コミットメッセージにはプレフィックス（`feat:`, `fix:`, `refactor:`, `docs:` 等）を用い、変更内容と意図を簡潔明瞭に記述すること。
- 未コミットの変更を溜め込まず、機能・論理単位でこまめにコミットを打つこと。

### ⑥ Cloudflare D1 ＋ 夜間バッチによる全銘柄DB化アーキテクチャ
- **背景と課題解決**:
  - 全上場銘柄（約4,000銘柄）の決算サマリーをフロントエンドから都度取得すると、J-Quants Lightプラン（60回/分）の制約により約65分の待機が発生する。
  - これを解消するため、**Cloudflare D1 (SQLite)** を導入し、平日夜間に **GitHub Actions** 上で稼働するバッチ処理（`batch/sync_daily.ts`）が当日の全開示・最新株価・バリュエーションを収集して財務11指標・ベータ値を事前計算し D1 へ格納。
- **データ取得仕様**:
  - **J-Quants 日付指定一括取得 (`/fins/summary?date=YYYY-MM-DD`)**: その日1日に適時開示・決算修正を出した全銘柄が1リクエストで返却される仕様を活用。日次バッチでは当日分（1リクエスト）のみで全開示を差分更新可能。
  - **スクリーナー配信 (`functions/api/screener-stocks.ts`)**: D1 のビュー `v_screener_stocks` をクエリし、約4,000銘柄のリアルタイム配当利回り・株価・時価総額等を 0ms で配信（Cloudflare CDNで5分間キャッシュ）。
  - **ウォッチリスト指標一括配信 (`functions/api/watchlist-metrics.ts`)**: D1 の `calculated_metrics` テーブルから事前計算済みの11指標・5期CF履歴・ベータ値を一括取得。ブラウザでの待機時間・プログレスバー表示を完全撤廃。
- **ローカル開発環境でのD1エミュレーション**:
  - `vite.config.ts` に組み込まれた `d1DevPlugin` が `node:sqlite`（Node.js組み込み）経由で `.wrangler/state/v3/d1` 内のローカル SQLite を直接読み取り、ローカル開発時（code-server / Vite）でも本番と完全に同一の D1 API エンドポイントをエミュレート。
  - フロントエンドは本番・ローカルともに常に `/api/screener-stocks` および `/api/watchlist-metrics` を参照する透過的な設計。

### ⑦ 改修フローの原則（事前提案・合意形成とGit運用）
- **修正前の提案・合意（絶対厳守）**:
  - ユーザーから課題の指摘や改修依頼を受けた際、いきなりコードを修正して事後報告することは厳禁。
  - **必ず事前に「原因の分析」「修正方針・修正案（対象ファイル、変更箇所のコード、影響範囲）」をユーザーに提示し、合意・確認を得てから実装に着手**すること。
- **Git運用の役割分担**:
  - **AIエージェントの担当**: 改修後のテスト（`npm test`）およびビルド（`npm run build`）の確認、および機能単位でのローカルGitコミット作成（`git commit`）まで。
  - **ユーザーの担当**: リモートリポジトリ（GitHub）へのプッシュ（`git push`）は、内容を確認した上でユーザー自身が実行する。AIエージェントは勝手に `git push` を行わないこと。

---

## 3. ディレクトリ構成と役割

```
stock-analyzer/
├── AGENTS.md                  # 本ファイル (AI向け仕様書・運用ルール)
├── README.md                  # 人間向けプロジェクト概要・デプロイ手順
├── index.html                 # エントリーHTML
├── wrangler.toml              # Cloudflare D1 設定 (Git除外, ローカル用)
├── wrangler.toml.example      # Cloudflare D1 設定テンプレート
├── tailwind.config.js         # Tailwind CSS設定
├── package.json               # 依存関係・スクリプト定義
├── .node-version              # Cloudflare Pages / CI用 Node.js バージョン指定 (v22)
├── .nvmrc                     # NVM用 Node.js バージョン指定 (v22)
├── .env                       # APIキー環境変数 (Git除外)
├── .env.example               # 環境変数テンプレート
├── .github/
│   └── workflows/
│       └── daily_sync.yml     # 平日夜間自動実行 (18:30 JST) GitHub Actions ワークフロー
├── schema/
│   ├── schema.sql             # Cloudflare D1 データベース定義 (stocks, quotes, metrics, views)
│   └── initial_seed.sql       # 過去250営業日の全上場銘柄初期シードSQL (約6.5MB)
├── batch/                     # 夜間バッチ・データ収集スクリプト群 (TypeScript)
│   ├── lib/
│   │   ├── jquantsClient.ts   # レートリミット制御付きJ-Quants APIクライアント
│   │   └── metricsCalculator.ts # 11指標・ベータ値事前計算ラッパー
│   ├── init_historical.ts     # 過去250営業日開示収集・初期シードSQL生成スクリプト
│   └── sync_daily.ts          # 平日夜間差分更新バッチ (D1直接書き込み対応)
├── functions/                 # Cloudflare Pages Functions (エッジ関数)
│   └── api/
│       ├── jq/
│       │   └── [[path]].ts    # 本番用 J-Quants API プロキシ
│       ├── watchlist.ts       # 本番用 サーバー共通ウォッチリスト API (Cloudflare KV)
│       ├── screener-stocks.ts # 本番用 全銘柄スクリーナー配信用 API (Cloudflare D1)
│       └── watchlist-metrics.ts # 本番用 ウォッチリスト事前計算指標 API (Cloudflare D1)
├── scripts/
│   └── update_universe.mjs    # JPX400マスターデータ更新・生成スクリプト
├── src/
│   ├── main.tsx               # Reactエントリーポイント
│   ├── App.tsx                # メイン画面レイアウト・タブ状態管理
│   ├── index.css              # Tailwind CSS ディレクティブ
│   ├── data/
│   │   ├── jpx400Data.ts      # JPX400構成銘柄マスターデータ
│   │   └── watchlist.json     # 開発環境用 サーバーウォッチリスト永続化ファイル
│   ├── types/
│   │   └── jquants.ts         # J-Quants APIレスポンス、スクリーナー・ウォッチリスト型定義
│   ├── services/
│   │   ├── jquantsApi.ts      # API連携、一括データ取得、レートリミット監視
│   │   └── cacheService.ts    # 二層キャッシュ管理 (インメモリ Map + localStorage 永続化)
│   ├── hooks/
│   │   ├── useStockData.ts    # 個別銘柄データ取得・状態管理カスタムフック
│   │   ├── useScreener.ts     # スクリーニングデータ取得 (D1優先 + フォールバック)
│   │   ├── useWatchlist.ts    # ウォッチリスト同期・指標横断結合
│   │   └── useWatchlistFinancials.ts # 事前計算指標一括反映 (D1優先 + 0ms描画)
│   ├── components/
│   │   ├── Header.tsx         # ヘッダー (ロゴ、API残枠、設定ボタン)
│   │   ├── NavigationTabs.tsx # タブ切替 (チャート分析 / スクリーニング / ウォッチリスト)
│   │   ├── StockSearchForm.tsx# 4桁コード入力フォーム、ウォッチリスト銘柄クイック選択
│   │   ├── StockSummaryCard.tsx # 株価・配当利回り・配当確定月/内訳・PER/PBR・時価総額カード
│   │   ├── StockChart.tsx     # TradingView Lightweight Charts (日足・SMA・出来高・決算E/配当Dピン留め)
│   │   ├── StockDividendHistory.tsx # 年間配当金推移バーグラフ・連続増配年数・配当性向テーブル
│   │   ├── ScreenerView.tsx   # 銘柄スクリーニング画面 (ユニバース切替・配当/指標フィルタ)
│   │   ├── WatchlistView.tsx  # ウォッチリスト比較画面 (タブ切替・11財務指標・5期CF縦並び)
│   │   └── ApiKeyModal.tsx    # APIキー設定 & キャッシュ削除モーダル
│   └── utils/
│       ├── indicators.ts      # SMA計算、財務サマリー解析(FCF/DOE/ROA等11指標)、配当履歴
│       └── formatters.ts      # 通貨・時価総額・キャッシュフロー(兆/億)・パーセントフォーマッタ
└── test/
    ├── unit.test.ts           # フォーマッタ・SMA・財務11指標算出・バッチ計算の単体テスト
    └── api_verify.mjs         # J-Quants API 実環境疎通確認スクリプト
```

---

## 4. 開発・検証コマンド

```bash
# 依存関係のインストール
npm install

# 開発サーバー起動 (code-server上の https://<domain>/absproxy/5173/ でアクセス)
npm run dev

# 単体テスト実行 (Node.js ネイティブテストランナー)
npm test

# プロダクションビルド (TypeScript型チェック + Viteバンドル -> dist/)
npm run build

# ローカル D1 データベースへのスキーマ適用・初期化
npx wrangler d1 execute jquants-db --local --file=schema/schema.sql

# ローカル D1 データベースへの初期シード投入 (過去250営業日開示データ)
npx wrangler d1 execute jquants-db --local --file=schema/initial_seed.sql

# ローカル D1 データベースの日次同期バッチ手動実行
npx tsx batch/sync_daily.ts

# 本番 D1 データベースの初期スキーマ適用 (初回のみ)
npx wrangler d1 execute jquants-db --remote --file=schema/schema.sql

# 本番 D1 データベースへの初期シード投入 (初回のみ)
npx wrangler d1 execute jquants-db --remote --file=schema/initial_seed.sql

# J-Quants 実APIとの疎通・データ整合性テスト
node test/api_verify.mjs
```

---

## 5. 機能仕様とデータフロー

### ① 銘柄スクリーニング機能
- **ユニバース切替**: JPX日経400（デフォルト）、TOPIX 100（大型優良株）、プライム市場、全銘柄。
- **データ取得と効率性**:
  - 初回アクセス時に全銘柄マスター（`/equities/master`、24時間キャッシュ）、最新営業日の日足株価（`/equities/bars/daily?date=...`）およびバリュエーション指標（`/equities/valuation?date=...`）を一括取得。
  - プライム市場や全銘柄ユニバースでも、全4,000銘柄超の正式会社名・業種・市場区分が100%完全に表示される。
  - `jpx400Data.ts` の直近配当金予想（またはキャッシュ内の最新公式開示 `extractLatestDps`）とリアルタイム終値を掛け合わせ、**リアルタイム配当利回り**を即座に算出。配当未確認銘柄に対する根拠のない推計値（EPS×35%等）は完全に排除し、公式開示の正確性を保証。
  - レートリミット（60回/分）を圧迫せず、クライアント側でミリ秒単位の複合フィルタリング（利回り、PER、PBR、時価総額、出来高、33業種、社名検索）を実現。
  - **初期デフォルト条件**: 配当利回り **2.5% 以上**、出来高 **500,000株（50万株）以上**（JPX日経400ユニバース）。「フィルターをリセット」時も本デフォルト条件に復元。
- **ブックマーク連携**: スクリーニング結果から個別または一括でウォッチリストへ登録可能。銘柄クリックでチャート画面へシームレス遷移。

### ② ウォッチリスト・多角財務指標比較機能 (持続力・還元方針・事業基盤)
- **公式開示配当金・配当利回りの完全自動同期**:
  - バックグラウンドで各銘柄の決算サマリー（`/fins/summary`）を取得（またはキャッシュ参照）した際、`extractLatestDps()` によって最新の会社予想配当金（`FDivAnn`）およびリアルタイム株価に基づく配当利回りを自動算出し、ウォッチリスト比較テーブルやソート、DOE・配当性向の計算に即座に反映。個別チャート分析画面で表示される配当利回りとウォッチリスト比較画面の配当利回りが常に100%一致するよう整合性を担保。
- **サーバー共通永続化 & 端末間自動共有**:
  - **アーキテクチャ**: どのPC・端末からアクセスしても常にサーバー上の単一共通ウォッチリストを参照・更新するコードレス・認証レス設計。
  - **本番環境 (Cloudflare Pages)**: `functions/api/watchlist.ts` が Cloudflare KV（バインド名: `WATCHLIST_KV`、固定キー: `shared_watchlist`）と通信し、エッジ上で高速・安全に読み書き。
  - **開発環境 (code-server / Vite)**: `vite.config.ts` の開発用ミドルウェアが `src/data/watchlist.json` にファイル永続化。ローカル環境でも複数ブラウザ間でシームレスに同期。
  - **キャッシュ & オフライン耐性**: 初回描画のちらつき防止およびオフライン時のフォールバックとして `localStorage` とインメモリ配列を併用。サーバー不通時でもアプリが停止せずローカル値で動作。
  - **UI連携**: ツールバーに「☁️ サーバー同期済 / 同期中」ステータスボタンを配置。クリックでサーバーの最新状態へ手動再同期可能。
- **カテゴリ別タブ切替UI**:
  1. **基本・株価**: コード、社名、業種、株価、出来高、時価総額、配当利回り、年間配当金、PER、PBR、ROE。
  2. **1. 持続力 (Sustainability)**:
     - キャッシュフロー分割: 直近期の **営業活動CF (CFO)**、**投資活動CF (CFI)**、および **フリーCF (FCF)** を独立した列として分割表示。
     - 過去5期キャッシュフロー推移テーブル (縦並び表示): 各銘柄の「5期内訳 ▼」ボタン、または上部「5期推移を全展開」トグルにより、過去5年間の各年度における [決算期 | 営業CF | 投資CF | フリーCF | CF創出タイプ判定] が縦に並んだ専用内訳テーブルをアコーディオン展開して一覧比較可能。
     - FCF恒常性: 過去5期FCF黒字年数（例: `5/5期プラス`）、恒常プラス判定 🛡️。
     - 過去配当履歴: 過去5期非減配年数（例: `5期減配なし`）、連続増配年数（`🔥 X期連続`）。
     - 自己資本比率: 40%以上健全、60%超「強固」バッジ判定。
     - 市場感応度・ベータ値 (β): 対TOPIXの1年ベータ値を算出して表示。β < 0.8 は「ディフェンシブ🛡️」、β > 1.2 は「景気敏感🚀」、0.8〜1.2 は「市場連動⚖️」判定。昇順・降順ソート対応。
  3. **2. 還元方針 (Return Policy)**:
     - 配当利回り & 1株配当金。
     - 配当性向: 30〜50%健全（緑）、50〜70%適正（青）、70〜100%警戒（黄）、100%超/赤字危険（赤）。
     - DOE (自己資本配当率): 2.5%以上「安定」、3.5%以上「最優良 ★」バッジ判定。
     - 自社株買い検知: 期末自己株式数の推移から積極還元姿勢を検知。
  4. **3. 事業基盤 (Fundamentals)**:
     - 営業利益率: 8%以上優良、10%以上「高収益 ★」バッジ判定。
     - ROE: 8%以上「合格」バッジ判定。
     - ROA: 5%以上「合格」バッジ判定。
     - EPS中長期成長性: 5年年平均成長率 (CAGR %) および成長基調判定。
- **全カラム昇順・降順ソート & タブ間ソート順維持**: 全タブの全新指標（ベータ値を含む）で即時ソートに対応。タブ（基本/持続力/還元方針/事業基盤/総合）を切り替えても選択中のソート条件・銘柄の並び順が勝手にリセットされず、完全に維持・踏襲される快適なUI設計。
- **主要財務指標の見方・投資判断目安ガイド (開閉アコーディオン)**: 上部ツールバーの「💡 指標の見方・目安」ボタンからワンクリックで展開・折りたたみ可能。「持続力 (FCF/営業CF/投資CF/自己資本比率/非減配年数/ベータ値)」「還元方針 (DOE/配当性向/自社株買い)」「事業基盤 (営業利益率/ROE/ROA/EPS成長)」の定義・計算式・投資適格目安を解説。選択中タブに連動して該当カテゴリをハイライト。
- **D1事前計算指標優先 ＆ 0ms即時描画 (待機プログレス完全撤廃)**:
  - ウォッチリスト表示時、D1 の `/api/watchlist-metrics` から事前計算済みの財務11指標を一括取得。
  - D1 に財務指標が存在する銘柄は即座に解決済み（`uncachedItems` に含めない）と判定され、「解析中...」のプログレスバーを一切出さずに 0ms で画面描画を完了。
  - ベータ値が未計算の場合でも画面描画をブロックせず、D1未登録銘柄の財務データ取得時のみプログレスバーを起動する安全なフォールバック設計。
- **ベータ値独立キャッシュ & 差分更新 (Diffing) による爆速化**:
  - **ベータ値独立キャッシュ (`cacheService.getBetaAnalysis` / `setBetaAnalysis`)**: スクリーナー等からウォッチリストへ追加された銘柄（個別チャート未閲覧銘柄）でも、算出されたベータ値（`BetaAnalysis`）を24時間有効な独立二層キャッシュに永続化。個別チャートを開かずとも次回のAPI通信・再計算を完全にゼロ化。
  - **銘柄削除時の処理スキップ (Diffing)**: `useWatchlistFinancials` 内で前回の銘柄コード集合と比較し、純粋な銘柄削除時は再フェッチ・再計算・プログレスリセットを完全にバイパス。削除ボタン押下時に 0ms で該当行が即座に除去される。
  - **モジュールレベルキャッシュによるタブ移動時の 0ms 復元**: モジュールスコープに `globalFinancialsCache` と `globalBetaCache` を保持。タブを行き来しても計算済み指標が維持され、全銘柄キャッシュ済みなら最初から `isLoading: false` のままプログレスバーを一切出さずに瞬時にテーブルを描画。
- **直接追加・管理**: 4桁コード手動追加、個別削除、全クリアをサポート。

### ③ 個別銘柄の年間配当推移・連続増配・配当スケジュール分析機能
- **追加通信ゼロ**: 個別株検索時に取得済みの財務サマリー（`/fins/summary`）から過去5年間の本決算（FY）レコードおよび最新会社予想（`FDivAnn`）を抽出し、追加のAPIコールなしで集約・可視化。
- **配当権利確定月・配当金内訳表示 (`DividendSchedule`)**:
  - 本決算・中間決算レコードの会計期間終了日（`CurFYEn` / `CurPerEn`）および配当実績・予想値（`Div2Q`, `DivFY`, `FDiv2Q`, `FDivFY` 等）から、各銘柄の配当スケジュールを完全自動解析。
  - **配当権利確定月**: 「3月末 / 9月末」「12月末 / 6月末」「1月末 / 7月末」「3月末 (年1回)」などを明示。
  - **中間・期末の内訳金額**: 「中間 (9月): 50円 (予想)」「期末 (3月): 50円 (予想)」「年間合計: 100円」を独立したチップで一目で把握可能。2Q開示済み銘柄（INPEXやJTなど）では中間配当の実績確定バッジと期末予想を自動区別。
  - 銘柄サマリーカード上部に専用情報バーを配置し、年間配当利回りカード内にも確定月を常時表示。
- **ビジュアル配当推移グラフ**: 年度ごとの年間配当金額（円）をバーグラフ表示（増配: 緑、据置: 青、減配: 赤、会社予想: 点線ストライプ）。
- **連続増配年数カウント**: 直近期・予想期から過去に遡り、連続して年間配当が増加している年数を自動計算して「🔥 X期連続増配」バッジを表示。
- **配当性向・EPS推移テーブル**: 年度ごとの1株配当（DPS）、前年比増減額（円）/増減率（%）、配当性向（%）、1株当たり利益（EPS）を一覧表示。ハイライトバー付きで直感的な把握が可能。

### ④ 市場感応度・景気サイクル分析 (ベータ値: β)
- **TOPIXベンチマーク連携**:
  - J-Quants API（`/indices/bars/daily/topix`）から東証市場全体のベンチマーク日足を取得（24時間キャッシュ保持、1日1回のみ通信）。
  - 個別銘柄の取得済み日足（`AdjC`）とTOPIX終値の日次リターンから、共分散・分散に基づく正確なベータ値（$\beta = \text{Cov}(R_i, R_m) / \text{Var}(R_m)$）および相関係数（$r$）をミリ秒単位で算出。
- **個別株サマリーでのビジュアル判定表示**:
  - 直近1年間（約250営業日）のβ値に加え、中長期（3年β・5年β）の感応度推移、TOPIX相関係数を表示。
  - **感応度グラデーションメーター**: 0.0（超低感応）〜 1.0（市場標準）〜 2.0+（超高感応）のバー上に現在位置をピン表示。
  - **自動判定バッジ**:
    - $\beta < 0.8$: **🛡️ ディフェンシブ株**（守りの資産・相場急落耐性・安定配当向き）
    - $0.8 \le \beta \le 1.2$: **⚖️ 市場連動株**（TOPIX連動・ニュートラル）
    - $\beta > 1.2$: **🚀 景気敏感株**（シクリカル・好況ブースト・高ボラティリティ）
  - **投資判断の視点解説**: 個々の銘柄のベータ特性に応じた投資活用指針を自動提示。

### ⑤ 高速キャッシュアーキテクチャ (インメモリ ＋ localStorage 二層構造)
- **背景と課題**: ウォッチリストの銘柄数が増加（数十〜100銘柄）した際、各銘柄の日足データ（約120KB/銘柄）に対する `localStorage.getItem` および同期的な `JSON.parse` の多発がレンダリングや進捗更新時のフレームレート低下（UIカクつき）の主要因となっていた。
- **Level 1: インメモリキャッシュ (`Map<string, CacheEntry<T>>`)**:
  - `cacheService.ts` 内に JavaScript メモリキャッシュ（`memoryStockCache`, `memoryFinsCache`, `memoryBetaCache`, `memoryMasterList`, `memoryTopixBars`）を配備。
  - 初回アクセス時のみ `localStorage` からデシリアライズし、同時にメモリへ保存。
  - 2回目以降の取得（タブ切替、ソート、進捗プログレス更新時など）はメモリキャッシュから同一オブジェクト参照を $O(1)$（約0.0001ms）で即時返却し、冗長な JSON パースとストレージ I/O を完全に排除。
- **Level 2: localStorage 永続化**:
  - ページ再読込や次回ブラウザ起動時のためにバックグラウンドで `localStorage` にも永続保存。
  - 各種 TTL（株価日足: 12時間、財務・ベータ・マスター・TOPIX: 24時間）の有効期限判定および安全ガード（SSR/Nodeテスト環境での `typeof localStorage !== 'undefined'` ガード）を完備。

### ⑥ タイポグラフィと金融データ表示最適化 (Inter + Noto Sans JP + tabular-nums)
- **フォントスタック (`tailwind.config.js`, `index.html`, `src/index.css`)**:
  - 欧文・数字: **Inter**（400, 500, 600, 700）- 世界水準の金融・SaaS UIフォント。
  - 日本語: **Noto Sans JP**（400, 500, 600, 700）- あらゆるOS（Windows, Mac, Linux）で統一された美しい日本語ゴシック体。
  - 等幅コード・記号: **JetBrains Mono** - 視認性の高いプログラミング・金融シンボル用フォント。
  - フォールバック: システムフォント（Hiragino Sans, Yu Gothic UI 等）を安全に指定。
- **Google Fonts プリロード & 非同期ロード**:
  - `index.html` 内で `preconnect` および `display=swap` を用い、レンダリングブロックを防ぎつつ瞬時にロード。
- **金融テーブル・数値最適化**:
  - `td`, `th`, `.tabular-nums`, `.font-mono` に `font-variant-numeric: tabular-nums` をグローバル適用。各数値の文字幅を均一にし、財務テーブルの桁ズレ・波打ちを防止。
  - `font-feature-settings: "palt" 1` による日本語のプロポーショナル詰め、`-webkit-font-smoothing: antialiased` による文字のにじみ防止を完備。
  - ガイド内の数式説明など日本語テキストに `font-mono` が誤適用されないよう整備。
- **ダークテーマ視認性・ハイコントラスト化**:
  - `body` のベースフォントウェイトを **`font-weight: 500` (Medium)** に底上げ。黒背景（`#0f172a`）上での文字の細字化錯視（かすれ）を根本解消。
  - グレー文字（`text-slate-400`, `text-slate-500`, `text-slate-600`）の明度を1〜2段階引き上げ、沈み込みを防止してくっきりと判読できるように最適化。
  - スクリーナー・ウォッチリスト・個別銘柄サマリーのテーブル見出し（`th`）や銘柄名・指標ラベルのフォントサイズと配色コントラストを強化。

### ⑦ チャート上のイベントピン留め機能（決算発表日 E / 配当情報 D）
- **TradingView公式プラグイン連携 (`createSeriesMarkers`)**:
  - `lightweight-charts` (v5.2.1) の `createSeriesMarkers` プラグインAPIを利用し、ローソク足チャート上に直感的なイベントピンを描画。
- **決算発表日マーカー (Earnings: E)**:
  - 財務サマリー（`/fins/summary`）の開示日（`DiscDate`）と決算期別区分（`DocType` / `CurPerType`）から「通期本決算」「1Q決算」「2Q中間」「3Q決算」を識別。
  - **同一決算期の重複排除**: 同一四半期・本決算において決算短信と四半期報告書・訂正開示等の複数開示が存在する場合でも、市場が最も反応する初回の決算発表日（短信公表日）のみを厳密に1件採用し、重複マーカーの乱立を完全に防止。
  - ローソク足の下部（`belowBar`）にパープル（`#a855f7`）のピン留めバッジとして表示。
- **配当発表日・金額マーカー (Dividends: D)**:
  - 過去の各期本決算で確定した実績年間配当（例: `D: 88円`）のみをローソク足の上部（`aboveBar`）にアンバー（`#f59e0b`）のピン留めバッジとして表示。未到来・進行期の会社予想配当（`FDivAnn`）は過去チャートの誤認を防ぐためマーカーから除外し、チャート下部の年間配当推移グラフおよび銘柄サマリーカードで適切に表示する。
- **休業日・時間外開示の自動営業日スナップ（二分探索）**:
  - 金曜夕方や土日・祝日に開示された適時開示情報であっても、株価チャート上の直後の有効営業日（`Date`）へ二分探索アルゴリズム（$O(\log N)$）で自動スナップ。日付ズレや非営業日指定による描画エラーを完全に防止。
- **表示切り替えトグル & 凡例ツールバー**:
  - チャート上部の操作パネルに「決算 (E)」「配当 (D)」それぞれのチェックボックスを配備し、ワンクリックで瞬時にピンの表示/非表示を切り替え可能。

---

## 6. 今後の拡張ロードマップ

1. **テクニカル指標の追加**:
   - `indicators.ts` に RSI、MACD、ボリンジャーバンドの計算ロジックを追加し、`StockChart.tsx` のサブペインに描画可能。
2. **Cloudflare D1 / Workers による完全リアルタイムDB化**:
   - 定期バッチで全上場銘柄の日足をDBに蓄積し、より高度な複数期間トレンド分析を可能にする。

